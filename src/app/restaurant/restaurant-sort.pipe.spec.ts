import { RestaurantSortPipe } from './restaurant-sort.pipe';

describe('RestaurantSortPipe', () => {
  it('create an instance', () => {
    const pipe = new RestaurantSortPipe();
    expect(pipe).toBeTruthy();
  });
});
